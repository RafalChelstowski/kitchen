# Feature: Kitchen Rapier Migration

- [x] Clean generated build output before migration | AC: untracked `dist/` is removed or ignored, `git status --short` shows no generated build output, no source files are changed by this task
- [x] Add Rapier and required React/Three stack versions | AC: `package.json` includes `@react-three/rapier@2.2.0`, `react`/`react-dom` are React 19 compatible, `@react-three/fiber` is 9.x compatible, `@react-three/drei` is 10.x compatible, `three` satisfies Rapier 2 peer range
- [x] Refresh lockfile after stack install | AC: `pnpm-lock.yaml` matches `package.json`, `pnpm typecheck` runs far enough to report source errors instead of missing packages, `@react-three/cannon` remains installed for incremental migration
- [x] Update app test mocks for the new stack while Cannon remains | AC: existing `App.test.tsx` still mocks active physics imports, no new test framework or browser test infrastructure is added, `pnpm test` passes
- [x] Replace Cannon Triplet type-only usage in non-body code | AC: `Letters.tsx` no longer imports from `@react-three/cannon`, local tuple type is used for animation positions, `pnpm typecheck` passes
- [x] Replace Cannon Triplet type aliases in body components | AC: body components use a local tuple type or Three-compatible tuple type, no component imports `Triplet` from `@react-three/cannon`, `pnpm typecheck` passes
- [x] Switch the root physics provider to Rapier | AC: `App.tsx` imports `Physics` from `@react-three/rapier`, dev debug uses Rapier `debug={import.meta.env.DEV}` or equivalent, Cannon `Debug` is removed from `App.tsx`, existing app smoke test passes
- [x] Migrate floor collider to Rapier | AC: `Floor.tsx` uses Rapier fixed rigid body/collider instead of `usePlane`, floor position and rotation match previous behavior, `pnpm typecheck` passes
- [x] Migrate static cube bounds to Rapier | AC: `CubeBoundary` uses Rapier fixed cuboid collision, GLTF-derived position/rotation/dimensions are preserved, cube boundary mesh names remain raycast-compatible for placement logic
- [x] Migrate static cylinder bounds to Rapier | AC: `CylinderBoundary` uses Rapier fixed cylinder collision, GLTF-derived position/radius/height are preserved, `StaticBounds` no longer imports Cannon hooks
- [x] Migrate window blocker to Rapier | AC: `InteractiveWindow` uses a Rapier fixed/kinematic collider instead of `useBox`, open state moves the blocker out of the window path, closed state restores the original blocker position
- [x] Migrate player body to Rapier | AC: `Player.tsx` uses a Rapier rigid body ref instead of `useBox`, pointer-lock movement still drives horizontal velocity, camera follows the body, rotations are locked/reset so the player does not tip over
- [x] Migrate Harnas can body to Rapier | AC: `Harnas.tsx` uses Rapier cylinder body/collider, hidden/picked/thrown states preserve position/velocity/rotation behavior, floor collision still unlocks `AchievementName.HARNAS`
- [x] Migrate Transform mug body to Rapier | AC: `Transform.tsx` uses Rapier cylinder body/collider, hidden/picked/animated/attached states preserve position/velocity/rotation behavior, ready-coffee reset collision behavior is preserved
- [x] Migrate Express grip body to Rapier | AC: `Express.tsx` uses Rapier cuboid body/collider, pickup/drop/attached/animated states preserve position/rotation/velocity behavior, coffee state transitions still typecheck
- [x] Migrate Mugs instanced bodies to Rapier | AC: `Mugs.tsx` uses Rapier instanced rigid bodies, initial grid placement is preserved, selected mug can still be positioned while picked and thrown with velocity
- [x] Migrate unmounted Rain instanced bodies to Rapier | AC: `Rain.tsx` no longer imports Cannon, falling can instances use Rapier instanced bodies or an equivalent Rapier-compatible implementation, component typechecks even though it is not mounted
- [x] Remove remaining Cannon imports from tests and source | AC: `rg "@react-three/cannon|useBox|useCylinder|usePlane|Triplet" src` returns no matches, app tests mock Rapier rather than Cannon, `pnpm test` passes
- [x] Remove Cannon dependency | AC: `@react-three/cannon` is removed from `package.json`, `pnpm-lock.yaml` is refreshed, `rg "@react-three/cannon" package.json pnpm-lock.yaml src` returns no active dependency/import matches
- [x] Update physics migration docs for completion | AC: `docs/physics-migration.md` states the project now uses `@react-three/rapier@2.2.0`, lists any behavior intentionally left equivalent rather than identical, removes obsolete instruction saying not to migrate in this pass
- [x] Fix source compatibility from React 19 / R3F 9 / Three upgrade | AC: any type or API break caused by the stack upgrade is fixed without changing gameplay scope, `pnpm typecheck` passes
- [x] Build the migrated app | AC: `pnpm build` passes, build no longer reports missing Cannon modules, any remaining Three/Rapier warnings are recorded under Findings
- [x] Final verification for Rapier migration | AC: `pnpm typecheck` passes, `pnpm test` passes, `pnpm build` passes, `rg "@react-three/cannon|useBox|useCylinder|usePlane|Triplet" src package.json` returns no Cannon runtime leftovers
- [x] Fix Three texture color-space warning in `src/features/kitchen/useKitchenGltf.tsx` | AC: `texture.encoding` is replaced with `texture.colorSpace`, color/albedo texture uses `THREE.SRGBColorSpace`, normal/metalness/roughness maps remain non-color data maps, `pnpm typecheck` passes
- [x] Resolve or document Rapier WASM init warning | AC: `@dimforge/rapier3d-compat@0.19.3` is tested through a pnpm override or direct compatible dependency resolution, browser console no longer shows using deprecated parameters for the initialization function or `docs/physics-migration.md` documents it as upstream noise, `pnpm build` passes
- [x] Fix first-person camera sensitivity only | AC: `src/features/player/Player.tsx` changes `PointerLockControls` from `pointerSpeed={0.2}` to `pointerSpeed={3.5}`, movement `SPEED = 3.5` remains unchanged, no settings UI or Zustand pointer-speed plumbing is added, `pnpm typecheck` passes
- [x] Fix achievement-toast crash on interactive clicks | AC: `react-toastify` is upgraded from `^8.2.0` to a React 19 compatible version such as `^11.1.0`, existing `ToastContainer` usage remains simple, first-time achievement interactions call `toast.success` without crashing, `pnpm test -- --run` passes
- [x] Remove unstable per-frame dynamic-body parking for Express grip static state | AC: Express grip `ATTACHED_EXPRESS` state no longer uses a dynamic mass-zero body forced by per-frame `setTranslation`, the parked grip is fixed/kinematic and stable when viewed in Rapier debug, no per-frame zeroing of velocity/angular velocity is needed for the attached state, `pnpm typecheck` passes
- [x] Convert Express grip animation states to kinematic scripted motion | AC: Express animated states use kinematic-position semantics with `setNextKinematicTranslation` and `setNextKinematicRotation`, direct per-frame `setTranslation`/`setRotation` is removed for scripted animation states, the grip/grinder/accessory animations still complete and update coffee state, `pnpm typecheck` passes
- [x] Convert Transform mug hidden/attached/animated states to kinematic or disabled semantics | AC: Transform mug hidden state is disabled or non-dynamic, attached/animated states are kinematic/scripted instead of dynamic mass-zero, dynamic mode is used only for free/thrown/dropped behavior, `pnpm typecheck` passes
- [x] Stabilize held item follow behavior for Harnas | AC: when picked, Harnas switches to kinematic-position mode, follows camera with `setNextKinematicTranslation` and `setNextKinematicRotation`, does not zero linear/angular velocity every frame while held, returns to dynamic mode on throw/drop, `pnpm typecheck` passes
- [x] Stabilize held item follow behavior for Express and Transform | AC: picked Express grip and Transform mug use the same kinematic-held approach as Harnas, held items stay stable during camera rotation and player movement, no gravity/damping/player-speed tuning is used to hide jitter, `pnpm typecheck` passes
- [x] Rework Harnas throw velocity | AC: Harnas throw uses fixed camera-forward launch velocity instead of raycast-hit distance, launch includes a small upward component, throw can plausibly reach the open window from normal play distance, `pnpm typecheck` passes
- [x] Add realistic physical material values for Harnas and hard surfaces | AC: Harnas body/collider has reasonable mass, linear damping, angular damping, friction, and restitution values, floor/static bounds have hard-surface friction/restitution values, thrown Harnas settles instead of rolling indefinitely, `pnpm typecheck` passes
- [x] Replace broken mug instanced rendering with explicit mug bodies | AC: Mugs renders 12 visible mug meshes using normal mesh + `RigidBody` components instead of relying on `InstancedRigidBodies`, each visible mug has a matching debug collider, GLTF node `toukMug1` and material `yellowToukCupMaterial` are still used, `pnpm typecheck` passes
- [x] Restore mug pickup/place/throw behavior on explicit mug bodies | AC: one mug can be picked, held, placed, and thrown; the visual mug and physics body remain synchronized; held mug uses kinematic-held behavior and dynamic mode only after throw/drop, `pnpm typecheck` passes
- [x] Remove stale pointer-speed settings residue if unused | AC: unused `PointerSpeedSlider` and unused persisted `pointerSpeed` state/types are removed only if no runtime code uses them after hard-coding camera sensitivity, store tests are updated only for removed state shape, `pnpm test -- --run` passes
- [x] Final browser regression pass for interactive physics | AC: opening the app in dev mode shows no React crash on interactive clicks, no texture color-space warning, no severe parked-object jitter in Rapier debug, Harnas throw is usable, mugs are visible and interactable, `pnpm build` passes
- [x] Add a shared held-item pose helper | AC: a reusable helper computes held world position and yaw rotation from the camera and per-item offset, helper uses reusable `THREE.Vector3`/`THREE.Euler`/`THREE.Quaternion` objects instead of React state, Harnas, Mugs, Express, and Transform can import it without circular dependencies, `pnpm typecheck` passes
- [x] Convert Harnas held rendering to camera-authoritative visual follow | AC: when Harnas is picked, the Rapier body is disabled with `setEnabled(false)` or its collider is removed from collision solving, the visible held can is rendered outside the `RigidBody` and follows the camera transform directly, the held visual does not participate in raycasting against placement/throw targets, `pnpm typecheck` passes
- [x] Sync Harnas body back to Rapier on place and throw | AC: placing the can re-enables the Rapier body at the placement point before setting `playerStatus` to null, throwing the can re-enables the Rapier body at the current held visual transform before applying launch velocity, no PICKED Harnas branch calls `setNextKinematicTranslation` or `setNextKinematicRotation` every frame, `pnpm test -- --run` passes
- [x] Convert regular mug held rendering to camera-authoritative visual follow | AC: selected mug visibility is split so the picked mug is shown by a non-physics held visual while its Rapier body is disabled or removed from collision solving, non-selected mugs remain normal dynamic Rapier bodies, held mug visual uses the same camera pose helper and is excluded from placement/throw raycasts, `pnpm typecheck` passes
- [x] Sync regular mug body back to Rapier on place and throw | AC: placing a held mug re-enables only the selected mug body at the clicked placement point, throwing a held mug re-enables only the selected mug body at the held visual transform before applying velocity, Mugs has no PICKED follow path that relies on per-frame `setNextKinematicTranslation`, `pnpm test -- --run` passes
- [x] Convert Express grip held rendering to camera-authoritative visual follow | AC: only the PICKED Express grip state uses a non-physics held visual, `ATTACHED_EXPRESS` and animated coffee workflow states remain Rapier kinematic/scripted as they are now, the picked grip Rapier body is disabled or removed from collision solving while held, `pnpm typecheck` passes
- [x] Sync Express grip body back to Rapier on place or attach target | AC: placing the grip on a static surface re-enables the Rapier body at the placement point and returns it to dynamic physics, attaching to express/grinder/accessories re-enables or repositions the body before entering the existing animated state, the PICKED Express branch no longer calls `setNextKinematicTranslation` every frame, `pnpm test -- --run` passes
- [x] Convert Transform mug held rendering to camera-authoritative visual follow | AC: only the PICKED Transform mug state uses a non-physics held visual, hidden/animated/attached coffee workflow states keep their current kinematic scripted behavior, the picked Transform Rapier body is disabled or removed from collision solving while held, `pnpm typecheck` passes
- [x] Sync Transform mug body back to Rapier on place, attach, and throw | AC: placing the Transform mug re-enables the body at the placement point, attaching to the espresso machine enters the existing animated coffee state from the held visual transform, throwing re-enables the body at the held visual transform before applying velocity, `pnpm test -- --run` passes
- [x] Remove obsolete picked-state kinematic follow code | AC: `rg "setNextKinematic" src/features/kitchen/Mugs.tsx src/features/kitchen/interactive/Harnas.tsx src/features/kitchen/interactive/Express.tsx src/features/kitchen/interactive/Transform.tsx` shows no PICKED camera-follow branches using `setNextKinematicTranslation` or `setNextKinematicRotation`, scripted non-held animation states may still use `setNextKinematic*`, `pnpm typecheck` passes
- [x] Final held-item regression check | AC: picking up Harnas, a regular mug, Express grip, and Transform mug no longer vibrates while moving with WASD, camera rotation still keeps the held item stable, place/drop/throw still returns each item to visible Rapier-driven world behavior, `pnpm build` passes
- [x] Fix held-object placement raycasts against nested bounds | AC: held-object placement raycasts for Harnas, regular mugs, Express grip, and Transform mug use recursive intersection or an equivalent helper so nested `static-*` meshes inside Rapier `RigidBody` wrappers are hit, placement onto static bounds works for all held item types, held non-physics visuals remain excluded from placement/throw raycasts, `pnpm typecheck` passes
- [x] Render transformed held Transform mug variant | AC: when `transformed` is true and the Transform mug is picked, the non-physics held visual renders the transformed mug meshes and ready coffee payload instead of disappearing, the normal untransformed held mug still renders correctly, `pnpm typecheck` passes
- [x] Keep coffee payload visible on held Express grip | AC: when the Express grip is picked after grinding and `coffeePortion` scale is visible, the held grip visual carries the coffee payload with the handle instead of leaving it on the disabled body branch, existing attached/animated coffee workflow behavior is preserved, `pnpm typecheck` passes
- [x] Final review-comment regression pass | AC: Codex review cases are covered locally: held items can be placed on bounds after Rapier wrappers, transformed Transform mug remains visible while held, grinded Express grip shows coffee while held, `pnpm test -- --run` and `pnpm build` pass

## Findings

- Codex PR review on #27 flagged three actionable regressions: placement raycasts skip nested Rapier-wrapped static bounds, transformed Transform mug disappears while held, and grinded Express grip loses the visible coffee payload while held.
- Current placement code still uses non-recursive `raycaster.intersectObjects(...)` on `scene.getObjectByName('bounds')?.children` in multiple held-object paths; because `StaticBounds` wraps meshes in Rapier bodies, recursive intersection is needed for `static-*` mesh hits.
- Held non-physics visuals intentionally define `raycast={() => undefined}` and should stay excluded from raycasts after the recursive placement fix.
- Next held-item pass should prefer camera-authoritative non-physics held visuals for PICKED states; Rapier bodies should be disabled while held, then synchronized back to physics on place, attach, throw, or drop.
- Keep existing kinematic/scripted coffee workflow states for Express and Transform; this pass targets only PICKED held follow behavior unless the task explicitly says otherwise.
- The old dynamic-body teleport issue has mostly been fixed already; current held objects use `kinematicPosition` and `setNextKinematicTranslation`.
- Rapier applies `setNextKinematicTranslation` on the next physics pipeline update, not immediately.
- `@react-three/rapier` defaults to fixed 1/60 physics plus interpolation, while the player camera and held-item targets are updated in render-frame callbacks.
- The vibration appears only during player movement because camera world position changes every frame; camera rotation is stable because the positional lag is much smaller.
- The recommended fix is to decouple visible held items from Rapier while held, then sync back to Rapier only on place, drop, or throw.
- Current branch is already on `@react-three/rapier@2.2.0`, React 19, R3F 9, Drei 10, and Three >=0.159.
- `react-toastify@8.2.0` is incompatible with the current React 19 runtime behavior and crashes when achievement toasts render.
- The Rapier init warning appears to come from `@react-three/rapier` calling `@dimforge/rapier3d-compat.init()` internally, not from app code.
- Held and parked physics objects are currently dynamic bodies that are manually teleported every frame; this is the main source of jitter.
- Do not change player movement speed; only camera pointer speed should change.
- Do not add new test infrastructure.
- React 19/R3F 9 compile compatibility required a local global JSX bridge in `src/types/react-jsx-compat.d.ts`, updating legacy `planeBufferGeometry`/`boxBufferGeometry` JSX tags, and using `.js` suffixes for Three example imports under bundler module resolution.
- App smoke tests now mock the active Rapier `Physics`; Cannon `Physics`/`Debug` is no longer imported by `App.tsx`.
- `corepack pnpm run build` passes after the root Rapier provider switch, with Vite's existing large chunk warning for the bundled app chunk.
- Rapier player migration uses a dynamic `RigidBody` with a manual cuboid collider, `lockRotations`, `setLinvel` for pointer-lock movement, and `setAdditionalMass(0/3)` to preserve the previous unlocked/locked mass behavior.
- Rapier Harnas migration uses a dynamic `RigidBody` with a manual `CylinderCollider`; floor collision checks both `other.colliderObject` and `other.rigidBodyObject` ancestors for the existing `floor` mesh name before unlocking `AchievementName.HARNAS`.
- Rapier Transform migration uses a dynamic zero-mass `RigidBody`, manual `CylinderCollider`, `setAdditionalMass(0/1)` for attached/manual vs thrown physics, and resets ready coffee on low-height collision using the body's Rapier translation.
- Rapier Express migration uses a dynamic zero-mass `RigidBody`, manual `CuboidCollider`, `setAdditionalMass(0/3)` for attached/animated/picked vs dropped grip physics, and keeps coffee state transitions inside the existing click/spring flow.
- Rapier Mugs migration uses `InstancedRigidBodies` with manual `CuboidCollider` half extents `[0.05, 0.04, 0.05]`; selected instances are controlled through the rigid-body ref array for picked positioning and throw velocity.
- Rapier Rain migration uses `InstancedRigidBodies` with a manual `CylinderCollider` args `[0.09, 0.08]`; the timer still respawns random can instances above the scene by setting their rigid-body translation.
- Final migrated build passes without missing Cannon module reports or Three/Rapier warnings; the only remaining production build warning is Vite's existing large chunk warning for the bundled app chunk.
- `@dimforge/rapier3d-compat@0.19.3` is resolved through a pnpm workspace override for `@react-three/rapier`; the deprecated WASM init warning text still exists in generated wrapper code for deprecated callers, but `@react-three/rapier@2.2.0` calls `init()` without arguments.
- Express grip `ATTACHED_EXPRESS` now uses a `kinematicPosition` body type and no longer has a per-frame parking branch; picked/drop states still use the existing dynamic scripted branches for later checklist items.
- Express grip `ANIMATED_EXPRESS`, `ANIMATED_GRINDER`, and `ANIMATED_ACCESSORIES` now switch to `kinematicPosition` and use `setNextKinematicTranslation`/`setNextKinematicRotation`; picked/drop behavior is still dynamic for later checklist items.
- Transform mug now starts as `kinematicPosition`; hidden, picked, animated, and attached states use `setNextKinematicTranslation`/`setNextKinematicRotation`, while placed/thrown states switch back to dynamic mass.
- Harnas now switches to `kinematicPosition` only while picked and follows the camera with `setNextKinematicTranslation`/`setNextKinematicRotation`; placement and throw switch it back to dynamic mode.
- Express grip now switches to `kinematicPosition` while picked and follows the camera with `setNextKinematicTranslation`/`setNextKinematicRotation`; Transform pickup/release now also updates the Rapier body type immediately like Harnas.
- Transform attach and throw now sync the disabled Rapier body from the non-physics held mug visual before entering animated coffee flow or applying launch velocity.
- Harnas uses `mass={0.35}`, `linearDamping={0.45}`, `angularDamping={2.5}`, collider friction `0.9`, and restitution `0.12`; floor/static bounds use hard-surface friction `0.85` and restitution `0.05`, and Harnas can now sleep after settling.
- Mugs now render explicit `RigidBody`/`mesh` pairs with per-mug `userData.mugIndex`; the remaining mug pickup task should convert held/released body modes instead of reintroducing instancing.
- Explicit mug bodies now switch the selected mug to `kinematicPosition` while held, use `setNextKinematicTranslation`/`setNextKinematicRotation` during follow, and switch back to dynamic for place/throw.
- Final browser regression used cached Playwright Chromium from `/workspace/.cache/ms-playwright` plus rootless Debian library extraction under `/workspace/browser-libs`; the dev app rendered with Rapier debug, pointer lock entered cleanly, the menu hid after lock, click/throw input produced no page errors, no texture color-space warnings appeared, and the only browser warnings were the documented Rapier init warning plus WebGL `ReadPixels` performance noise from screenshots.
- Pointer lock state now derives from `document.pointerLockElement === gl.domElement` instead of blindly toggling on `pointerlockchange`, preventing the menu/crosshair state from drifting if the browser emits extra lock-change events.
- Harnas PICKED now disables its Rapier body and renders `harnas_held` as a non-raycast mesh updated directly from the shared held-pose helper; place/throw re-enable the body before returning to dynamic world behavior.
- Regular mugs now disable only the selected Rapier body while held, hide that body's mesh, and render a non-raycast held visual directly from the shared held-pose helper; existing place/throw handlers re-enable the selected body.
- Express PICKED now disables the Rapier body, hides the body-owned grip mesh, and renders `int-grip-held` from the shared held-pose helper; the next sync pass should copy that held visual transform back before attach animations.
- Express attach/place now syncs the disabled Rapier body from `int-grip-held` before animation or dynamic placement; grinder/accessories return-to-picked disables the body again and shows the non-physics held visual.
- Express held grip now renders a non-physics `coffeePortion` mesh when coffee state is `grinded` or `tempered`; the body-owned payload is hidden while held so it does not remain on the disabled body branch.
